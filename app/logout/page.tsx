

const LogoutPage = () => {
    // Perform logout actions here
    // For example, clear session, cookies, or JWT tokens

    return (
        <div>
            <h1 color="black">Logout Page</h1>
            <p>You have been logged out. <a href="/login">click to login</a></p>
        </div>
    );
};

export default LogoutPage;
